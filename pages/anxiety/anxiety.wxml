<view class="container">
  <view class="list">
    <block wx:for="{{articles}}" wx:key="_id">
      <view class="item" bindtap="navigateToAnxietyPage" data-item="{{item}}">
        <view class="title">{{item.title}}</view>
      </view>
    </block>
  </view>
</view>