<view class="container">
  <view class="list">
    <block wx:for="{{articles}}" wx:key="_id">
      <view class="item" bindtap="navigateToMassagePage" data-item="{{item}}">
        <view class="title">{{item.title}}</view>
      </view>
    </block>
  </view>
</view>