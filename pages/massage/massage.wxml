<view class="container">
  <view class="list">
    <block wx:for="{{urls}}" wx:key="index">
      <view class="item">
        <a href="{{item.url}}">
          <text>{{item.title}}</text>
        </a>
      </view>
    </block>
  </view>
</view>
